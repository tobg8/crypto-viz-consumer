import { Typography, styled } from '@mui/material';

export const TypographyTitle = styled(Typography)({
  fontWeight: 400,
  fontSize: '1rem',
  color: 'rgb(156 163 175)',
});

export const TypographyTitlePrice = styled(Typography)({
  fontWeight: 400,
  fontSize: '1rem',
});
